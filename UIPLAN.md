# SaveMoney — UI-план: экраны, вкладки, функции

Как выглядит приложение и что где находится. Статусы: ✅ готово (Фаза 1), 🔜 в какой фазе будет.
Навигация — Shell TabBar внизу, 4 вкладки; стартовая — «Ввод» (требование скорости: расход с первого экрана).

## 1. Карта навигации

```
TabBar (внизу, всегда доступен)
├── 💸 Ввод        — быстрый ввод операции (стартовый экран)
├── 📋 История     — лента операций по дням + фильтры
├── 📊 Отчёты      — диаграммы, периоды, экспорт            (Фаза 3)
└── ⋯  Ещё         — счета, категории, долги, бюджеты,
                     «до зарплаты», настройки, синк

Push-страницы (внутрь стека):
Ещё → Счета → Счёт (создать/править)
Ещё → Категории → Категория (создать)
Ещё → Долги → Долг (карточка + платежи)                        (Фаза 2)
Ещё → Бюджеты                                                  (Фаза 2)
Ещё → До зарплаты (дашборд)                                    (Фаза 2)
История → тап по операции → Ввод (режим правки)
Отчёты → тап по сектору → История с фильтром категории
```

## 2. Дизайн-принципы

| Принцип | Решение |
|---|---|
| Скорость | Старт в «Ввод» < 1 с; клавиатура сразу; расход за 2 касания (см. §3) |
| Числа главнее | Суммы крупно и жирно; вторичный текст приглушён (opacity 0.5–0.7) |
| Цвета операций | Расход — красный `#E74C3C`, доход — зелёный `#2ECC71`, перевод — синий `#3498DB`, акцент — `#512BD4` |
| Иконки | Эмодзи (0 МБ шрифтов, кроссплатформенно); у категорий и счетов свой эмодзи/цвет |
| Темы | Светлая/тёмная (системная по умолчанию) — Фаза 3 |
| Язык | RU-first; resx-локализация перед стор-релизом |
| Пустые состояния | Каждая пустая страница объясняет первый шаг («создайте счёт», «добавьте операцию») |

## 3. Вкладка «Ввод» ✅ + полировка 🔜

```
┌──────────────────────────────────┐
│         Баланс: 48 250,00 ₽      │  ← общий по счетам с «учитывать в итоге»
│ ┌────────┬────────┬────────┐     │
│ │ Расход │ Доход  │Перевод │     │  ← сегменты, активный — акцентный
│ └────────┴────────┴────────┘     │
│                          1 250   │  ← крупно 42pt, справа
│ ┌──────┬──────┬──────┐           │
│ │  1   │  2   │  3   │           │
│ │  4   │  5   │  6   │           │
│ │  7   │  8   │  9   │           │
│ │  ,   │  0   │  ⌫   │           │
│ └──────┴──────┴──────┘           │
│ Счёт    (Сбер 💳)(Нал 💵)(Т-Банк)│  ← чипы, горизонтальная лента
│ Категория (🛒 Продукты)(🚗 Транз)│  ← корни; у корня с детьми:
│   └ (Магнит)(Пятёрочка)(Рынок)   │     раскрывается лента подкатегорий
│ Магазин [Пятёрочка_________]     │  ← необязательно; по нему
│ Заметка [__________________]     │     угадывается категория
│ Дата [12 сен 2026 ▾]             │
│ [         СОХРАНИТЬ          ]   │
└──────────────────────────────────┘
```

Умное поведение (✅ готово): последняя категория каждого типа подставляется сама;
введённый магазин угадывает категорию по прошлым покупкам; каждое сохранение
обучает правила «магазин → категория».

Полировка скорости (Фаза 3/4):
- 🔜 Настройка «сохранять сразу при выборе категории» → расход = цифры + тап по иконке (2 касания).
- 🔜 Панель «частые» — 4 последних категории крупными иконками над клавиатурой.
- 🔜 Счёт выбирается автоматически по правилу мерчанта (карта, которой обычно платят в этом магазине).
- 🔜 Режим «правка» тот же экран, заголовок «Расход · правка», кнопка удаления внутри.

## 4. Вкладка «История» ✅

```
┌──────────────────────────────────┐
│ [Неделя ▾]  [Все счета ▾]        │  ← фильтры: период, счёт
│ Доход 120 000 ₽ · Расход 71 750 ₽│  ← итог за выбранный фильтр
│ ┌──────────────────────────────┐ │
│ │ Сегодня    +120 000  −3 450  │ │  ← заголовок дня + суммы
│ ├──────────────────────────────┤ │
│ │ 🛒 Продукты         −1 250   │ │
│ │    Сбер · Пятёрочка          │ │  ← счёт · магазин · заметка
│ │ 🔁 Сбер → Наличные      −5 000│ │  ← перевод
│ ├──────────────────────────────┤ │
│ │ Вчера                −12 300 │ │
│ │ 🚗 Транспорт           −450  │ │
│ │    Наличные · Такси          │ │
│ └──────────────────────────────┘ │
│  ← свайп влево = удалить (мягко) │
│  ← тап = редактировать           │
└──────────────────────────────────┘
```

🔜 Полировка (Фаза 3/4): поиск по тексту/магазину; фильтр по категории; лента
черновиков парсера сверху (v1.1, см. §10).

## 5. Вкладка «Отчёты» — Фаза 3

```
┌──────────────────────────────────┐
│ Период [Этот месяц ▾] [Календарь]│  ← включая свой цикл (смены)
│                                  │
│ Баланс: 48 250 ₽                 │
│ Доход 120 000 │ Расход 71 750    │
│                                  │
│      ◉ Круговая по категориям    │
│   🛒▓▓▓ 34%  🚗▓▓ 17%  🏠▓▓ 15%  │
│                                  │
│ Топ категорий:                   │
│ 🛒 Продукты    24 300  ▓▓▓▓▓ 34% │
│ 🚗 Транспорт   12 100  ▓▓▓  17%  │
│ 🏠 ЖКХ          8 500  ▓▓   12%  │
│                                  │
│ Динамика расходов по дням ▁▂▄█▂▁ │  ← столбцы/линия
│ Средний расход в день: 2 390 ₽   │
│                                  │
│ [Экспорт CSV]  [Экспорт PDF]     │
└──────────────────────────────────┘
```
Функции: произвольный период (в т.ч. пользовательский цикл смен), сравнение с
предыдущим периодом, средние, экспорт. CSV бесплатен всегда, PDF — подписка (v2.0).

## 6. Вкладка «Ещё» (меню) ✅ каркас

```
┌──────────────────────────────────┐
│ Счета           наличные, карты ✅│
│ Категории       дерево, иконки ✅ │
│ Долги           мне должны / я… 🔜│  ← Фаза 2
│ Бюджеты         лимиты, план/факт🔥│  ← Фаза 2
│ До зарплаты     дашборд       🔜 │  ← Фаза 2
│ Настройки и экспорт           🔜 │  ← Фаза 3
│ Синк и семья                  🔜 │  ← v2.0
│ SaveMoney 1.0 · Фаза 1           │
└──────────────────────────────────┘
```

## 7. Счета ✅

```
Список:                          Карточка счёта:
┌──────────────────────────┐    ┌──────────────────────────┐
│ Всего: 48 250 ₽      [＋] │    │ Название [Сбер карта   ] │
│ ●💳 Сбер      42 300 ₽   │    │ Тип [Наличные/Карта/Банк]│
│   карта                  │    │ Начальный остаток [ 0  ] │
│ ●💵 Наличные  5 950 ₽    │    │ Цвет ●●●●●●●●            │
│   наличные               │    │ [x] Учитывать в балансе  │
│ ●💳 Т-Банк    −7 950 ₽   │    │ [ ] Архивный             │
│ ●🏦 Вклад   150 000 ₽    │    │ [Сохранить] [Удалить]    │
│  свайп = удалить         │    └──────────────────────────┘
└──────────────────────────┘
```
Баланс счёта = начальный остаток + доходы − расходы ± переводы. Архивный скрыт
из ввода, но виден в истории. «Учитывать в балансе» выключают для кредиток/вкладов.

## 8. Долги — Фаза 2

```
┌──────────────────────────────────┐
│ [Мне должны] [Я должен]      [＋]│  ← два списка, направление
│ Аня Петрова            5 000 ₽   │
│   до 20 сен  ▓▓▓░░ 2 000/5 000   │  ← прогресс погашения
│ Петя Сидоров          12 000 ₽   │
│   ⚠ просрочено (10 сен)          │
│                                  │
│ Карточка долга (тап):            │
│ Кто: Аня · Сумма: 5 000 ₽        │
│ Счёт для возврата: [Сбер 💳]     │
│ Платежи: 12.08 — 1 000 ₽         │
│          01.09 — 1 000 ₽  [＋]   │
│ → платёж по долгу может создать  │
│   транзакцию на связанном счёте  │
└──────────────────────────────────┘
```
Долг закрывается автоматически при полном погашении (логика готова в Core, ✅ тесты).

## 9. Бюджеты и «До зарплаты» — Фаза 2

```
Дашборд «До зарплаты» (карточка сверху отчётов или отдельный экран):
┌──────────────────────────────────┐
│ До зарплаты (5-е число): 22 дня  │
│ Остаток:            12 400 ₽     │
│ Предстоящие платежи: −8 500 ₽    │  ← регулярные до ЗП (ЖКХ, связь)
│ Можно тратить:      3 900 ₽      │
│ ≈ 177 ₽/день                   ✅│  ← индикатор: норм / мало / минус
└──────────────────────────────────┘

Бюджеты (лимит на цикл, а не только календарный месяц):
┌──────────────────────────────────┐
│ Цикл: 12.09 → 11.10 (30 дн) [▾]  │  ← якорь + длина = сменный график
│ Общий      71 750 / 60 000 ⚠ 119%│
│ 🛒 Продукты 24 300 / 20 000 ⚠    │
│ 🚗 Транспорт 12 100 / 10 000 ⚠   │
│ 🏠 ЖКХ       8 500 /  9 000 ✓    │
│                              [＋]│
└──────────────────────────────────┘
```
Механизм периода уже в схеме: `period_start_anchor_unix` + `period_days`
(30 = месяц, 14 = смена, 15 = полмесяца) — один механизм для бюджетов,
«до зарплаты» и отчётных периодов.

## 10. Настройки — Фаза 3

Тема (светлая/тёмная/системная) · День зарплаты · Цикл (якорь + длина) ·
Валюта (₽, позже другие) · «Сохранять сразу при выборе категории» (§3) ·
Экспорт/импорт CSV · Резервная копия файла БД (шаринг файла) ·
Локализация · «О приложении», политика «данные не блокируются никогда».

## 11. Виджет быстрого ввода — Фаза 4 (Android)

```
┌────────────────────────────────────┐
│ [＋100] [＋500] [＋1000] [✎ своё] │  ← 4×1, RemoteViews
└────────────────────────────────────┘
```
Тап по сумме → открывается «Ввод» с предзаполненной суммой (осталось выбрать
категорию — 2 касания). [✎] → пустой «Ввод». Дедупликация не нужна — источник
транзакции помечается `source=widget`.

## 12. Парсер уведомлений — v1.1 (Android, платная фича)

```
История, баннер сверху:
┌──────────────────────────────────┐
│ 🔔 2 новых операции из уведомлений│
│ ┌──────────────────────────────┐ │
│ │ −530 ₽ · MAGNIT              │ │  ← распознано: сумма, магазин,
│ │ 🛒 Продукты · Сбер  [✓][✗]   │ │     угаданная категория, счёт
│ └──────────────────────────────┘ │
└──────────────────────────────────┘
```
Источник — `NotificationListenerService` (пуши банковских приложений + СМС-уведомления;
READ_SMS не запрашиваем). Подтверждённое сохраняется как обычная транзакция
(`source=notification`), отклонённое — в `notification_log`, чтобы не показывать снова.
Экран онбординга: кнопка «Разрешить чтение уведомлений» + объяснение зачем.

## 13. Синк и семья — v2.0

```
Ещё → Синк и семья
┌──────────────────────────────────┐
│ Аккаунт: ivan@example.com  [выйти]│
│ Синхронизация: 5 мин назад  [⟳]  │
│ Семья «Ивановы» (3 участника)    │
│  👤 Иван (владелец)              │
│  👤 Жена (может вводить)         │
│  👤 Сестра (только просмотр)     │
│ [Пригласить по коду]             │
└──────────────────────────────────┘
```
Схема БД уже sync-ready (GUID, `updated_at`, `is_deleted`) — конфликт решается
last-write-wins, удаления — томбстоунами. Подписка: парсер + синк + PDF ~99 ₽/мес.

## 14. Сводка функций по экранам

| Экран | Функции | Статус |
|---|---|---|
| Ввод | Расход/доход/перевод, клавиатура, чипы, угадывание, правка | ✅ |
| История | Группировка по дням, фильтры, свайп-удаление, правка | ✅ |
| Счета | CRUD, баланс каждого, общий баланс, архив | ✅ |
| Категории | Дерево, добавление с иконкой, удаление своих | ✅ |
| Отчёты | Круговая, динамика, произвольный период, CSV/PDF | Фаза 3 |
| Настройки | Тема, ЗП/цикл, экспорт/бэкап, локализация | Фаза 3 |
| Виджет | Ввод с локскрина за 2 касания | Фаза 4 |
| Долги | Мне должны/я должен, платежи, автозакрытие | Фаза 2 |
| Бюджеты | Лимиты по категориям и общий, план/факт | Фаза 2 |
| До зарплаты | Остаток − предстоящие, ₽/день, циклы | Фаза 2 |
| Черновики уведомлений | Распознавание, подтверждение, дедуп | v1.1 |
| Синк/семья | Аккаунт, общие данные, роли | v2.0 |
